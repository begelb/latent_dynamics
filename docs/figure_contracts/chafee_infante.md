# fig_chafee_infante

Paper section `sec:chafee_infante`: Chafee-Infante PDE in spectral coordinates
(64 Fourier modes), super-critical regime `α = 28`.

## Paper figures

By published file name:

- `ci_bif_diagram.pdf` (bifurcation diagram, static)
- `ci_morse_representation_full.pdf` (`fig:ci_MRfull`)
- `ci_morse_representation_coarse.pdf` (`fig:ci_MRfull`)
- `ci_latent_1d_morse_graph.pdf` (`fig:ci_latent_1d`)
- `ci_latent_1d_morse_sets.pdf` (`fig:ci_latent_1d`)
- `ci_morse_graph.pdf` (`fig:ci_morse_graph_dynamics`)
- `ci_morse_sets.pdf` (`fig:ci_morse_graph_dynamics`)
- `ci_coarse_morse_graph.pdf` (`fig:ci_morse_graph_dynamics`)
- `ci_coarse_morse_sets.pdf` (`fig:ci_morse_graph_dynamics`)
- `ci_latent_3d_conley_morse_graph.pdf` (`fig:ci_latent_3d`)
- `ci_latent_3d_morse_sets_cubical_no_legend.pdf` (`fig:ci_latent_3d`)
- `ci_attractor_basins.pdf` (`fig:ci_attractor_basins`)
- `morse_roa_overlay.pdf` (`fig:ci_attractor_basins`)

The classification tables of the appendix
(`tab:ci_dimension_roa_statistics`, `tab:basins_attraction`) are covered by
[`chafee_roa_statistics.md`](chafee_roa_statistics.md); the residual/tolerance
rows by [`sampled_residual_tolerance.md`](sampled_residual_tolerance.md).

## Source of paper run

The reference d=2 Chafee-Infante computation is the coauthor's original run:
model weights, training data, training script, CMGDB script, and the exact
fine-panel PDFs were provided by the model's author. The converted weights
and data ship in the fetched artifact bundle:

- data: `replay_sources/chafee_infante/data/train_data.csv` (30,000 one-step
  pairs; also converted to `data/train.csv` for the package pipeline)
- model: `replay_sources/chafee_infante/replay/models/autoencoder.pt`
  (converted copy of the author checkpoint)
- CMGDB: data-derived latent bounds with 10% per-axis expansion,
  `subdiv_init=14`, `subdiv_min=16`, `subdiv_max=22`, and `padding=True`

The one- and three-dimensional panels come from the saved latent-dimension
study (locally trained on the same author data by
`scripts/chafee_latent_dimension_study.py`):

- `replay_sources/chafee_infante/latent_dimension_study/latent_1d/seed_0/`
- `replay_sources/chafee_infante/latent_dimension_study/latent_3d/seed_0/`

The active three-dimensional graph is byte-identical to the level-palette
render produced by `scripts/render_chafee_infante_3d_graph_palette.py`.

## Static and hand-drawn panels

`ci_bif_diagram.pdf` is an author-provided static figure (matplotlib PDF).
No generating script or source data for it survives; it is a static asset.
It remains contextual manuscript artwork, and the companion repository makes
no regeneration claim.

`ci_morse_representation_full.pdf` and `ci_morse_representation_coarse.pdf`
are pure renderings of the known theoretical Morse representations (11
equilibria ordered by unstable-manifold dimension; 3-node bistable
coarsening). They are generated by `scripts/render_chafee_theoretical_morse.py`
using the paper palette; no NN or CMGDB computation is involved.

## Status

**The coauthor's original d=2 computation is the production source.** Its
fine adaptive DOT/CSV never persisted — only the author's rendered PDFs
survive — so the fine panels `ci_morse_graph.pdf` and `ci_morse_sets.pdf` are
geometry-preserving palette recolors of those author PDFs
(`scripts/recolor_chafee_pdf.py`) and are **not recomputable byte-exactly**.
The individual recolor invocations were not logged; the mapping is fixed by
the script's constants.

The package replay at `replay_sources/chafee_infante/replay/` uses the
converted author weights; it is retained for replay and for the residual
computation and must not be used to generate the manuscript's collapsed
Morse set with non-production CMGDB settings.

The saved three-dimensional adaptive stage remains a topology-only fallback
in its status record. Its 30,672 saved Morse boxes all lie on the common
level-24 cubical grid, so the Conley indices have been computed post hoc from
verified local uniform index pairs using those exact boxes and the persisted
lookup map. This preserves the original stage provenance while supporting the
annotated active paper graph.

## Reproduction commands

Inspect the current converted-data config without recomputing CMGDB:

```bash
python pipeline.py --config chafee_infante --stages diagnose --max-seeds 1
```

Reconstruct the author's original cell graph and compute the
connection-complete coarse Morse representation:

```bash
python scripts/coarsen_chafee_infante.py
```

Render the theoretical Hasse diagrams and the standardized panels:

```bash
python scripts/render_chafee_theoretical_morse.py
python scripts/render_chafee_infante_standardized.py
```

## Expected scientific output

The two-dimensional reference computation shows seven Morse sets: two
attractors `(x-1, 0, 0)`, three saddles `(0, x-1, 0)`, and two repellers
`(0, 0, x-1)`. This statement is scoped to the two-dimensional computation;
the one- and three-dimensional study outputs have different node counts.

## Latent-dimension study

The saved one-dimensional adaptive computation uses bounds
`[-7.7618739128112795, 7.715793323516846]`, subdivisions `7/8/11`, and
padding. It computed a three-node Conley--Morse graph with edges `2 -> 0` and
`2 -> 1` and indices `(x-1, 0)`, `(x-1, 0)`, and `(0, x-1)`. The paper panels
`ci_latent_1d_morse_graph.pdf` and `ci_latent_1d_morse_sets.pdf` are
byte-identical to the study's saved `MG_adaptive` renders.

The saved three-dimensional adaptive computation uses bounds
`[-4.127716660499573, -2.7807257175445557, -3.2104987144470214]` to
`[3.8589504957199097, 2.5888736248016357, 3.2438191413879394]`, subdivisions
`21/24/33`, and padding. Its raw graph has 11 nodes, 14 edges, and minimal
nodes 0 and 1.
The authoritative inputs are
`replay_sources/chafee_infante/latent_dimension_study/latent_3d/seed_0/MG_adaptive/morse_graph`
(SHA-256
`1b032a5cda8f53b8adb8bcc8460991ab7c857ba87d052e1ac0e7da2967848156`)
and the corresponding `morse_sets` file (SHA-256
`14979bd3f3cf526e24a7a486822e0c48328b93bfc57d374cf0709682c2370919`).
The source-level annotated DOT has SHA-256
`47d6c6682251f4533f37ba1b566e94b3012fdffe3e7c312040ee664171851b8d`.
The active annotated panel `ci_latent_3d_conley_morse_graph.pdf` is
byte-identical to the standardized Conley--Morse render and has SHA-256
`3fbe39b57b984985ef74d35489aedc8daa384b28f8106a1e53e264046dc45d54`.

The computed indices, ordered by node, are:

| node | Conley index `(H0,H1,H2,H3)` |
|-----:|--------------------------------|
| 0 | `(x-1, 0, 0, 0)` |
| 1 | `(x-1, 0, 0, 0)` |
| 2 | `(0, x-1, 0, 0)` |
| 3 | `(0, x-1, 0, 0)` |
| 4 | `(0, 0, 0, 0)` |
| 5 | `(0, 0, 0, 0)` |
| 6 | `(0, 0, x-1, 0)` |
| 7 | `(0, x-1, 0, 0)` |
| 8 | `(0, 0, x-1, 0)` |
| 9 | `(0, 0, x-1, 0)` |
| 10 | `(0, 0, 0, x-1)` |

Two complete audit runs reproduced every index and index-pair signature. In
each run all 2,952 fiber acyclicity checks passed, and all 2,070
Morse-reduced preboundary solves passed an explicit
`boundary(result) == input` validation. The repository build reproduced all 11
indices once more.

### Cubical d=3 Morse-set panel (open item)

The published `ci_latent_3d_morse_sets_cubical_no_legend.pdf` was a one-off
no-legend export (2026-08-05) whose exact invocation was not persisted; no
byte-identical copy exists in any run directory. The data source is
established (the `latent_3d/seed_0` `MG_adaptive` Morse sets pinned above,
level-palette coloring). The `--no-legend` variant of
`scripts/render_chafee_infante_3d_graph_palette.py` regenerates the panel
visually identically, but not byte-identically.

## Coarse Morse representation

For the manuscript's bistability-level view, the five nonminimal nodes can be
collapsed to a single fiber while the two attractors remain separate. Following
the manuscript notation, the merged node is named `M(1)`: it represents the
nine unstable equilibria and the connecting orbits between them at the level of
the target coarse Morse representation.

```bash
python scripts/coarsen_chafee_infante.py
```

The resulting projection is `0 -> 0`, `1 -> 1`, and `{2,3,4,5,6} -> 2`;
the quotient Hasse edges are `2 -> 0` and `2 -> 1`. The script verifies that
the induced quotient is acyclic, recomputes the directed CMGDB cell graph from
the author's raw checkpoint and training data, and draws the merged set with
the cell-graph connections between the fine components in its fiber.

The fine Morse sets come from the adaptive run at (14, 16, 22), but the merged
fiber is completed on a **uniform** cell graph rebuilt at `subdiv_min = 16` --
256x256 in the plane, which is the grid the attraction-basin computation uses.
The five fine sets carry over to 1,594 uniform recurrent cells and path
completion adds 5,504 connection cells, giving 7,098 cells in `M(1)`, with no
overlap with the two singleton attracting sets.

Completing the fiber on the adaptive graph instead gives 1,515 + 2,702 = 4,217
cells, but its deepest cells trace connecting orbits at a resolution the
surrounding structures do not share: 7.4% of that enclosure falls inside an
attraction basin, against 0.18% for the uniform completion. That is why the
uniform construction is the published one.

The quotient has `M(1)` pointing to both attracting nodes, corresponding to
`M(0-)` and `M(0+)`. It intentionally assigns no Conley index to the merged
node: that index would have to be recomputed from an index pair for the union.

Connection completion is always enabled for the default (author-computation)
mode.

### Active basin overlay

The paper's basins figure overlays the coarse Morse representation on the
author's original uniform-grid regions of attraction. Regenerate it with:

```bash
python scripts/plot_chafee_coarse_morse_roa_overlay.py
```

This reproduces the separate `16/16/16` uniform basin computation on the same
data-derived bounds, draws the two uniquely assigned basins translucently, and
then overlays the connection-complete adaptive `M(1)` and the two attracting
Morse sets. Expected invariants: exactly 2 uniform attractors among 137
uniform Morse nodes on the 256 x 256 grid; basin cell counts 15,941
(positive) / 15,764 (negative), recorded in the run manifest
`morse_roa_overlay.json`.

When the live CMGDB `MapGraph` and Morse graph are available (during the Morse
computation, rather than from the saved DOT/CSV alone), use
`compute_connection_complete_morse_sets(map_graph, morse_graph, projection)`.
For each quotient fiber this computes the intersection of the forward-reachable
downset and reverse-reachable upset of its fine Morse cells. Thus it adds
exactly the cell-graph vertices on paths between Morse components mapped to the
same coarse node, while excluding connections from `M(1)` to `M(0-)` or
`M(0+)`. `write_connection_complete_morse_sets(...)` writes those augmented
cell sets in the CSV format used by the plotting code. The saved `morse_graph`
and `morse_sets` files do not retain the transient cell-graph edges, so this
path completion cannot be reconstructed from those two artifacts after the
fact.

## Hyperparameter audit

Archive values below come from the author's original training and CMGDB
scripts (`autoencoder_model.py`, `train_model.py`, `compute_dynamics.py`; not
distributed with this release).

| param                       | archive value           | YAML value             | source                                        | notes |
|-----------------------------|-------------------------|------------------------|-----------------------------------------------|-------|
| arch.encoder hidden_shapes  | [64, 32]                | [64, 32]               | src/latentdynamics/configs/chafee_infante.yaml | ✓     |
| arch.latent_map hidden_shapes | [32, 32]              | [32, 32]               |                                               | ✓     |
| arch.decoder hidden_shapes  | [32, 64]                | [32, 64]               |                                               | ✓     |
| arch.activation             | tanh                    | tanh                   | autoencoder_model.py:82,84,90,92,98,100       | ✓     |
| arch.encoder_out_activation | none                    | none                   | autoencoder_model.py:85 (Linear, no terminal) | ✓     |
| arch.latent_out_activation  | none                    | none                   | autoencoder_model.py:93                       | ✓     |
| arch.decoder_out_activation | none                    | none                   | autoencoder_model.py:101                      | ✓     |
| arch.high_dims              | 64                      | 64                     |                                               | ✓     |
| arch.low_dims               | 2                       | 2                      |                                               | ✓     |
| training.epochs             | 4000                    | 4000                   | autoencoder_model.py:73                       | ✓     |
| training.learning_rate      | 0.003                   | 0.003                  | autoencoder_model.py:72                       | ✓     |
| training.batch_size         | 30000 (full batch)      | 30000                  | author training script                        | ✓     |
| training.scheduler_factor   | 0.5                     | 0.5                    | autoencoder_model.py:113                      | ✓     |
| training.scheduler_min_lr   | 1e-6                    | 1e-6                   | autoencoder_model.py:115                      | ✓     |
| training.loss_weights       | recon + pred only       | [1, 1, 0]              | train_model.py:32,37 (no semiconjugacy term)  | ✓     |
| data.scaling                | none                    | none                   | no scaler in the author archive               | ✓     |
| data.n_samples_train        | 1000                    | 1000                   | autoencoder_model.py:13                       | ✓     |
| data.n_iterations           | 30                      | 30                     | autoencoder_model.py:14 (time_steps)          | ✓     |
| data.tau                    | 0.1                     | 0.1                    | autoencoder_model.py:12                       | ✓     |
| data.sampling_method        | uniform (seed 7206)     | uniform                | autoencoder_model.py:40,55                    | ✓     |
| cmgdb.subdiv_init           | 14                      | 14                     | compute_dynamics.py:58                        | ✓     |
| cmgdb.subdiv_min            | 16                      | 16                     | compute_dynamics.py:56                        | ✓     |
| cmgdb.subdiv_max            | 22                      | 22                     | compute_dynamics.py:57                        | ✓     |
| cmgdb.lower_bounds          | encoded-data min - 10%  | null (data-derived)    | compute_dynamics.py:31-40,60                  | ✓     |
| cmgdb.upper_bounds          | encoded-data max + 10%  | null (data-derived)    | compute_dynamics.py:31-40,61                  | ✓     |
| cmgdb.bounds_epsilon_frac   | 0.10                    | 0.10                   | compute_dynamics.py                           | ✓     |
| cmgdb.padding               | true                    | true                   | compute_dynamics.py:53-54                     | ✓     |

## Verification

```bash
python scripts/coarsen_chafee_infante.py
# Expected:
#   7 fine Morse nodes (adaptive 14,16,22)
#   M(1) = 1,594 recurrent + 5,504 connection cells on the uniform 256x256 grid
#   no coarse-set overlaps
```
