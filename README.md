Steps:

- Make a config file, samples are in ```config```
- Make data, e.g. with ```make_data.py```
- Scale the data with ```scale_data.py```
- Train autoencoder and latent dynamics with ```train.py```
- Compute Morse graph with ```morse_graph.py```
