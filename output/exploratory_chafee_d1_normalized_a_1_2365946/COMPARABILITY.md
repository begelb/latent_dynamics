# Normalized first-Fourier-coordinate cubic map

This is exploratory and not paper-eligible.

The fitted mu uses only the 30,000 training pairs. The fixed mu=0.75 comparator is predetermined. The additional scan uses the archived basin labels for post-hoc selection and must not be interpreted as held-out performance.
